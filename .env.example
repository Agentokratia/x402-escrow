# =============================================================================
# x402 Escrow Facilitator - Environment Variables
# =============================================================================

# Supabase (Required)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your-service-role-key

# JWT Secret for dashboard session tokens (Required)
# Generate with: openssl rand -base64 32
JWT_SECRET=

# WalletConnect Project ID (Required for wallet connections)
# Get from: https://cloud.walletconnect.com
NEXT_PUBLIC_WALLETCONNECT_ID=

# =============================================================================
# CDP Server Wallet (Required for on-chain operations)
# Get from: https://portal.cdp.coinbase.com
# The facilitator account is auto-created with name "x402-escrow-facilitator"
# =============================================================================

CDP_API_KEY_ID=your-cdp-api-key-id
CDP_API_KEY_SECRET=your-cdp-api-key-secret
CDP_WALLET_SECRET=your-cdp-wallet-secret

# =============================================================================
# Optional Configuration
# =============================================================================

# Vercel Cron Secret (Auto-set by Vercel, or set manually for local testing)
# See: https://vercel.com/docs/cron-jobs
CRON_SECRET=

# Capture threshold in USDC units (Optional, default: 1000000 = $1.00)
# When pending amount exceeds this, triggers batch capture
CAPTURE_THRESHOLD=1000000

# =============================================================================
# Note: Network configuration (RPC URLs, contract addresses) is stored in the
# 'networks' database table, not environment variables. See supabase/schema.sql
# for the seeded network values (Base Mainnet and Base Sepolia).
# =============================================================================
